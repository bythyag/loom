schema_version = 1

[runtime]
results_dir = "results"
traces_dir = "traces"

[ollama]
base_url = "http://127.0.0.1:11434"

[openrouter]
base_url = "https://openrouter.ai/api/v1"
# The API key is intentionally absent. Set OPENROUTER_API_KEY in the process
# environment or in an ignored local .env file.
